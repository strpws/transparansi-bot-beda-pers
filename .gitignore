# Large source asset — the optimized logo.png (256px) is what ships at runtime
Logo-Ikon-Kompas.id big.png

# Local Claude Code preview/dev config
.claude/

# Local-only cross-project notes (kept off the shared repo)
FLUID-LAYOUT.md
